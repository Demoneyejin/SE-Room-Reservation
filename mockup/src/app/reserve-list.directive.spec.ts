import { ReserveListDirective } from './reserve-list.directive';

describe('ReserveListDirective', () => {
  it('should create an instance', () => {
    const directive = new ReserveListDirective();
    expect(directive).toBeTruthy();
  });
});
